import pygame 

class.Background(pygame.sprite.Sprite):

  def __init__(self,image file)
    super().__init__()
    self.image =         pygame.image.load(background1.png).convert_alpha
pygame.image.load(background2.png).convert_alpha
pygame.image.load(background3.png).convert_alpha
  self.rect = self.image.get_rect()
  self.rect.y = 0 
  self.rect.x = 0
random.rand(pygame.image.load)
